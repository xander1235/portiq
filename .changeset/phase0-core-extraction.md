---
"portiq": minor
---

Extract a headless @portiq/core package (data store, request execution for HTTP/GraphQL/WebSocket, mock server, DAG flow engine, scripting/test harness, protocol handlers, curl import) and rewire the Electron main process to consume it. No user-facing behavior change.
